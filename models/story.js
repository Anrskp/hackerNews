class Story {
    constructor(id, title, url, timestamp, score, authorID) {
        this.id = id
        this.title = title;
        this.url = url;
        this.timestamp = timestamp;
        this.score = score;
        this.authorID = authorID;
    }
}

export default Story;